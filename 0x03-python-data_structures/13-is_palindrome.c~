#include "lists.h"

int is_palindrome(listint_t **head)
{
	unsigned int len = 0;
	listint_t *current;

	current = *head;
	while (current != NULL)
	{
		current = current->next;
		len++;
	}
	printf("Len: %d", len);
}
